# 面试讲稿：企业智能 IT/HR 助手 Agent

> 用途：Agent 应用开发岗（约 8k 档）现场演示与自我介绍。建议配合本地/容器化服务实机演示。

---

## 一、30 秒项目简介（开场）

> "这是一个面向企业内部的 **IT/HR 智能助手 Agent**。员工用自然语言提问，比如'年假怎么算''VPN 连不上帮我建个工单'，它会先做**意图路由**：简单问题用轻量小模型秒回，复杂问题才交给主模型，结合 **RAG 检索公司制度** 和 **调用业务工具**（查员工、查资产、查假期、建工单、转人工）给出带出处的可信答复。技术上后端 FastAPI，前端 React，模型统一走硅基流动，可一键 Docker 部署。"

---

## 二、技术架构（一页讲完）

- **双模型分级路由**：`Qwen2.5-7B` 做意图识别 + 简单直答；`DeepSeek-V3` 处理复杂任务。→ 成本/延迟优化，这是企业级最看重的点。
- **RAG 工程化**：文档分块（标题分段 + 滑动窗口 overlap，避免上下文在边界被切断）→ 云端 `bge-m3` **批量嵌入**（batch=16）→ Chroma 持久化；检索时向量召回 top_k 候选，再用**交叉编码器重排序（硅基流动 rerank）**精排 top_n，显著提升相关段落命中率，rerank 不可用时自动降级。
- **Agent 闭环**：手写 Function-Calling 循环（非只会调库），支持多轮工具调用 + 会话记忆 + SSE 流式。
- **全链路异步化**：`AsyncOpenAI` + 异步生成器 `arun()`，LLM 网络 IO 不阻塞事件循环，单实例即可并发处理多用户请求；文件 IO 用 `asyncio.to_thread` 卸载。
- **工程完整度**：配置中心化（pydantic-settings，模型名零硬编码）、Docker 部署、pytest 覆盖、前端明暗主题 + 引用/工具可视化 + **助手消息 Markdown 渲染**（react-markdown + remark-gfm）。

---

## 三、演示脚本（现场走一遍）

1. 打开页面 → 问"你好，你是谁" → 展示**轻量速答**路由标识 + 秒回（证明路由生效）。
2. 问"张伟的年假还剩几天" → 展示 **路由=智能助手**、RAG 引用卡片、工具调用徽章（查员工→查假期）→ 最终答复"剩余 7 天"。
3. 问"VPN 连不上帮我建个 IT 工单，优先级高，工号 E1001" → 展示**自动建单**返回工单号 `IT-xxxx`。
4. 点右上角切换**明暗主题**，展示工程细节。

> 演示前确认：`docker compose up --build` 已起服务，或本地 `uvicorn src.api.main:app --port 8000` + 前端 `npm run build` 已就绪。

---

## 四、高频追问 & 应答（提前准备）

- **Q：为什么不用单一大模型直接答？**
  A：小模型路由把 60%+ 闲聊/简单问题挡在主模型之外，单条成本与首字延迟都显著下降，是企业落地降本的关键设计。

- **Q：RAG 怎么保证不错？**
  A：向量检索 + 分数阈值过滤 + 强制标注出处；工具类实时数据绝不靠模型编造，必须走 function calling。

- **Q：工具怎么接真实系统？**
  A：当前是 Mock 后端，但接口签名已按真实 HR/IT 系统设计，`mock_db.py` 里每个函数都留了 `TODO(生产)` 的真实 HTTP 调用示例（北森/Moka/ITSM 类）。替换实现即可，Agent 逻辑零改动。

- **Q：会话记忆怎么存？**
  A：当前内存字典，生产可换 Redis；意图/工具链路与存储解耦，便于横向扩展。

- **Q：模型能换吗？**
  A：全部从 `.env` 读取，换硅基流动任意模型不改代码。

- **Q：嵌入为什么用云端 bge-m3？**
  A：统一接入、免去本地 torch 重依赖；入库走批量嵌入 + 哈希增量，运行时只嵌用户一句话，开销极低。

- **Q：为什么要把整个链路做成异步（AsyncOpenAI + arun）？**
  A：LLM 调用是主要耗时（数百毫秒到数秒的网络 IO）。同步写法会占住 worker 线程，并发一高就排队卡死；改成异步后，事件循环在等待模型响应时能去服务其他请求，单实例就能扛住多用户并发，且取消/新消息打断旧请求（代次令牌）也更干净。同步版 `run()` 仍保留作单测兼容。

- **Q：RAG 检索质量怎么进一步提升？**
  A：两件事——分块用滑动窗口 overlap 保留跨块上下文；召回后加一层交叉编码器重排序（rerank）做精排，把真正相关的段落顶到前面。rerank 模型不可用时自动降级为向量分数排序，保证链路不中断。

---

## 五、个人收获（收尾）

> "这个项目让我把 Agent 从'调 API'提升到'工程化落地'：路由分层、RAG 检索质量、工具编排与可观测，都是企业真正关心的。它也验证了我对成本、稳定性、可部署性的权衡意识。"

---

## 六、一句话电梯陈述（备用）

"我做了一个企业 IT/HR 智能助手：用双模型路由做成本优化，RAG + 工具调用做可信答复，FastAPI + React 全栈、Docker 一键部署，并预留了真实 HR/IT 系统的接入示例。"
